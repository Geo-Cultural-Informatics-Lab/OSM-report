# Core dependencies
pandas>=1.5.0
numpy>=1.23.0
geopandas>=0.12.0
shapely>=2.0.0
requests>=2.28.0
aiohttp>=3.8.0
tqdm>=4.64.0
scipy>=1.17.0
# REQUIRED Subproject dependencies (must be installed in editable mode)
# These are REQUIRED - the system will not fall back to mock data
# Run these commands from the report directory:
#   pip install -e ../geometric_complexity
#   pip install -e ../tags_semantic_analysis
# Or from the OSM root directory:
#   pip install -e geometric_complexity
#   pip install -e tags_semantic_analysis
